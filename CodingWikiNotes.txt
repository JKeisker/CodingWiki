Entity Framework Core w/ MVC Web App
Code-First Migrations
Reference project: CodingWiki

SECTIONS I & II: INTRO & MIGRATIONS
--------------------------------------------------------------------------------------------------------------------
1) Create solution w/ 3 projects (.Net7):
	a) DataAccess
		-Packages: Microsoft.EntityFrameworkCore.SqlServer(7.0.20)
			   Microsoft.EntityFrameworkCore.Tools(7.0.20)
		-Projects: Models
	b) Models
		-Packages: Microsoft.AspNetCore.Mvc.ViewFeatures(2.3.9)
	c) Web
		-Packages: Microsoft.EntityFrameworkCore.Design(7.0.20)
			   Microsoft.EntityFrameworkCore.Proxies(7.0.20)
			   Microsoft.VisualStudio.Web.CodeGeneration.Design(7.0.12)
		-Projects: Models, DataAccess


2) Create ApplicationDbContext.cs in DataAccess project
	In ApplicationDbContext 
		a) create DbSet<modelname> for each model that should be a Table in the DB.
		b) use [Key] data annotation for primary key of table
		b) add db connection reference in method 'OnConfiguring(...)

3) Add Migration
	a) set DataAccess project as startup project
	b) set PMC's to same project as 'a)' = DataAccess startup project
	c) Make desired changes to Model project
	d) If new table, add as DbSet to ApplicationDbContext
	e) PMC: add-migration AddBookToDB
	f) PMC: update-database

4) Remove Migration (***If you run this AFTER update-database for the migration you want to remove,
			it will screw up your code)
	a) ***Important: only use remove-migration when you are unhappy w/ a migration 
			AND have not pushed it to the DB.
	b) To remove last migration => remove-migration
		Now the unwanted migration will removed, and ApplicationDbContextModelSnapshot.cs will have been 
		updated to previous value/state. ***HOWEVER, in your model, it will still be set to the changes you were
		trying to set w/ the migration you just removed!!!
	

***5) IF you do run a migration and update-database w/o for example creating a DbSet in ApplicationDbContext,
	then you must follow these steps:
	a) Revert DB (roll back to prev state): Update-Database <PreviousMigrationName>
		***<PreviousMigrationName> : use class name w/o the date/timestamp prefix
	b) Run : remove-migration

6) NOTE: NEVER arbitrarily remove a migration unless you know what you're doing!
	ONLY make changes in the models and ApplicationDbContext!!

7) Rolling back to old migrations (ONLY applies to EF Core..AND NEVER DO THIS IN PRODUCTION THE TABLES WILL BE GONE!!!!)
	a) update-database NameOfMigrationWeWantToGoBackTo
	b) To return to previous state, just run 'update-database' and all reversions will be undone.
	
8) ***Most conservative/safe thing is to just add migrations rather than removing or rolling back. Pretty much
	every issue can be fixed by simply creating new migrations rather than using other methods.

9) More commands: 
	a) get-migration
	b) drop-database
	c) update-database - after running drop-database, this cmd will bring it back

10) Data Seeding
	Use ApplicationDbContext => OnModelCreating(ModelBuilder modelBuilder)		
				    {
					modelBuilder.Entity<Book>.Property(u=>u.Price).HasPrecision(10,5);
					modelBuilder.Entity<Book>.HasData(
						new Book { BookId = 1, Title = "SpiderMan", ISBN = "123B12", Price = 10.99m }
				    };

--------------------------------------------------------------------------------------------------------------------

SECTION III: DATA ANNOTATIONS

Chp 30: EF Core Power Tools

Do not check dab-options.json into source control if it contains sensitive connection strings, as this risks leaking credentials.  However, the configuration file dab-config.json (which stores the Data API Builder configuration) should generally be stored in source control for reproducibility, while the generated dab-build.cmd file is part of the scaffolding process. 

Here are the specific guidelines for these files:

dab-config.json: This file stores the Data API Builder configuration and is intended to be stored in source control.  It allows the API definition to be versioned and shared among developers.
dab-build.cmd: This file is generated by EF Core Power Tools to install the DAB CLI and build the configuration. It is a build script that can be checked in, but note that the tool aims to avoid hand-editing this file so it can be regenerated if the source schema changes. 
Security Warning: If your project generates an .env file containing connection strings (e.g., dab-connection-string=...), you must exclude this file from source control.  Add the following line to your .gitignore file:
.env

--------------------------------------------------------------------------------------------------------------------

SECTION V: FLUENT API

Definition - Fluent API is another way to configure the domain classes aside from 'Data Annotations'.
To use Fluent API, you need to do that in the ApplicationDbContext method 'OnModelCreating(ModelBuilder modelBuilder)' override.
Classes copied under 'FluentModels' folder to demo fluent api.

Fluent API also includes it's way of creating table relationships, instead of using Data Annotations.

Rather than clutter everything together in 'OnModelCreating(ModelBuilder modelBuilder)', you can create a 'FluentConfig' folder
in DataAccess project and add config files for each domain model class. These config classes need to  inherit from 
IEntityTypeConfiguration<(table model name)>.. See other config schema elements from examples in this CodingWiki solution.

Then under 'OnModelCreating(ModelBuilder modelBuilder', you only need to reference each fluid api config w/ 
modelBuilder.ApplyConfiguration(...)

If both Fluent API and Data Annotations are used w/ a particular model entity, if there is any contradiction Fluent API will be used.
These hybrid model entities might not need their own config as above and just have fluent api elements added directly under OnModelCreating().


SECTION VI: FILTERING QUERY

Good examples in Console app of common db queries using dbcontext, LINQ w/ C# for EF Core.

Important note: when using dbcontext and EF Core, parameters should be set to variables and then variable used in query - for security
purposes

Use .Find function to retrieve record by primary key

When you expect on 1 item returned from query, you can use .Single or .SingleOrDefault.. if more than 1 item is found, it will
return an exception

Use .Contains function when searching for a string or substring value - it will return all matches

Use Like function to set more precise sql-like searches => context.Books.Where(u=>EF.Functions.Like(u.ISBN, "12%"));
You can use EF.Functions.  to build multi-layered sql-like search as well

Interesting to know: 'var books = context.Books.ToList();' - the ToList() forces query execution right away the list is set to books
		     'var books = context.Books;' will not execute until the books variable iteration occurs in the 'foreach' loop (deferred 
		      execution)

When you have multiple .OrderBy and/or .OrderByDescending functions, only the last will be executed and the previous will be ignored! :)
You must use .ThenBy or .ThenByDescending to have multiple ordering conditions

For implementation of Pagination on website, use Skip() and Take()


To update db records: 1) retrieve record : var books = context.Books.Find(1003); 
		      2) set new value: books.ISBN = "777";
	              3) save changes to db: context.SaveChanges();

To delete a db item: 1) context.Books.Remove(book);
		     2) context.SaveChanges();

	
SECTION VII: EF CORE CRUD

chp70 - Setup EF Core in Web Project:

1) Register services in CodingWiki_Web's Program.cs file

   builder.Services.AddDbContext<ApplicationDbContext>(options =>
   {
       options.UseLazyLoadingProxies().UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection"));
   });

2) Add DB connection string to appsettings.json and comment out connection string in ApplicationDbContext

3) add constructor to ApplicationDbContext that passes option to base class:

	public ApplicationDbContext(DbContextOptions<ApplicationDbContext> options) : base(options) { }

4) Comment-out DB access in ApplicationDbContext's 'OnConfiguring' method


chp71 - Seed Category & Logging in EF Core:

(alternative to seeding data in AppDBContext's OnModelCreating())

1) add-migration seedCategory1 ***empty migration
			       ***since we now have setup DB connection from appsettings.json and other steps above in
				  "Setup MVC Web App to use EF Core", and have commented out access from ApplicationDbContext's
				  'OnConfiguring' method, WE NOW SET CodingWiki_Web as the startup app, but in PMC still have 
				   Default project set to CodingWiki_DataAccess

2) In 'Up' method of empty migration, write insert statements: migrationBuilder.Sql("INSERT INTO Categories VALUES ('Cat 6')");

3) In PMC run 'Update-Database'


***Drawback of above migration seeding approach - if at some point you decide to delete all migrations and add a new migration, you
may forget the migrations that were manually seeded as example above. That is why the preferred approach is always to simply
seed data and create migrations from AppDBContext's OnModelCreating()

4) Add 'Logging' config to appsettings.json


chp 72 - Retrieve using EF Core

   Create new controller in CodingWiki_Web
   At top reference AppDBContext and set instance w/ dependency injection : 

	private readonly ApplicationDbContext _db;

	public CategoryController(ApplicationDbContext db)
	{
    		_db = db;
	}
	
   ----------------------
   View logging here:  Debug -> Windows -> Output


chps 73-82 : PRACTICE CRUD operations w/o copying and pasting


SECTION VIII : BOOK MANAGMENT

chp 84. Projections in EF Core --> BookController.cs line 47
	
	BookVM obj = new();
	obj.PublisherList = _db.Publishers.Select(i => new SelectListItem 
	{ 
    		Text = i.Name,
    		Value = i.Publisher_Id.ToString()
	});

chp 87. Explicit Loading in EF Core, see BookController, search text 'explicit'
	
chp 91 Eager Loading, see BookController search text 'eager'



	